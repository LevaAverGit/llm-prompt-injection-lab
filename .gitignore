# Virtualenvs
.venv/
.venv-verify/
venv/
env/

# Python bytecode / caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Environment / secrets
.env

# Planning notes (kept local, not published)
PLAN.md

# OS / editor cruft
.DS_Store
.idea/
.vscode/

# The global core.excludesFile ignores prompts/ (Lev's local AI-prompt files);
# here prompts/ is a source package, so keep it tracked in this repo.
!prompts/
